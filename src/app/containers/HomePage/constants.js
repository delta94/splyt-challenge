// @dev this is to indicate where action are dispatch in the containers folders
const prefix = 'HomePage';

const GET_SPLYT_DRIVERS_START = `${prefix}/GET_SPLYT_DRIVERS_START`;
const GET_SPLYT_DRIVERS_SUCCESS = `${prefix}/GET_SPLYT_DRIVERS_SUCCESS`;
const GET_SPLYT_DRIVERS_ERROR = `${prefix}/GET_SPLYT_DRIVERS_ERROR`;
const SET_LOCATION = `${prefix}/SET_LOCATION`;
const SET_COUNT = `${prefix}/SET_COUNT`;

export {
  GET_SPLYT_DRIVERS_START,
  GET_SPLYT_DRIVERS_SUCCESS,
  GET_SPLYT_DRIVERS_ERROR,
  SET_LOCATION,
  SET_COUNT,
};
